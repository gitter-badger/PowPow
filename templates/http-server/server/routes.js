/*
 * Handle the routing if you decide
 * to have a templating engine.
 *
 * otherwise just delete this file.
 */

/*

module.exports = function (app) {
  app.get("/", function (req, res) {
    res.render("index");
  });
};

*/
